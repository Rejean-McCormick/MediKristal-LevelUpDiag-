# Sample evidence

`2026-09-22/` contains evidence captured while validating the MediKristal final archive used to build this profile.

The `software` campaign passed all required software-specific levels and retained only a context WARN because the extracted delivery was not a Git repository. Individual `MK*` results include delivery/release checks that intentionally expose the stale documentary evidence and coverage mismatch described in `../SAMPLE_VALIDATION_MEDIKRISTAL_FINAL.md`.

These files are examples for this exact archive, not authoritative evidence for any future build.
