[project]
name = "levelupdiag-medikristal"
version = "1.0.0"
description = "Target-specific LevelUpDiag validation suite for MediKristal"
requires-python = ">=3.10"
dependencies = []

[tool.levelupdiag]
mode = "standalone-medikristal-diagnostics-suite"
